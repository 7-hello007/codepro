# 项目计划书：基于 HiSH 构建鸿蒙代码编辑器（IDE）

**项目名称**：HiSH-IDE  
**目标平台**：HarmonyOS（手机、平板、2in1/PC）  
**文档版本**：2.0  
**编制日期**：2026-08-31

---

## 1. 项目总目标

在 **HiSH** 开源项目的基础上，通过**最小化改动原始代码**的方式，将其扩展为一款具备类 VSCode 布局的轻量级代码编辑器。最终产品将集成 Linux 终端（HiSH 原生能力）、文件管理（侧边栏）和代码编辑（Monaco Editor 封装组件），为鸿蒙开发者提供一个便携的移动端开发环境。

**核心原则**：**对 HiSH 原有文件尽可能不修改，通过新建组件、新增页面和配置来实现新功能**，以降低维护成本、保持与原项目的兼容性，并便于后续升级。

---

## 2. 设计原则（最小改动策略）

- **文件级隔离**：所有新增代码放在新建文件中，不修改原有文件内容（除非绝对必要）。
- **路由替换**：通过修改 `Index.ets` 中的路由判断，引入新布局组件，而非重写原有布局函数。
- **组件复用**：直接使用 HiSH 的 `WebTerminal` 和 VM 管理类，不修改其内部逻辑。
- **配置扩展**：利用 `module.json5` 的 ability 配置，可新增一个单独的 ability 来承载 IDE 模式，与原 HiSH 能力共存。
- **依赖最小化**：仅添加必要的第三方库（如 HMCodeEditor），并保持在 `oh-package.json5` 中独立声明。

---

## 3. 分阶段目标与执行计划

---

### 阶段一：环境搭建与源码准备（预计 1 天）

**目标**：获取 HiSH 源码，搭建开发环境，确认可编译运行。

| 步骤 | 具体操作 | 备注 |
| :--- | :--- | :--- |
| 1.1 | 安装 DevEco Studio 5.0+，配置 HarmonyOS SDK 6.0.2。 | 确保 Native 支持已安装。 |
| 1.2 | 克隆 HiSH 仓库：`git clone https://github.com/harmoninux/HiSH.git`，并拉取子模块。 | 子模块包含 QEMU 相关代码。 |
| 1.3 | 复制 `build-profile.template.json5` 为 `build-profile.json5`，下载必需资源（libs.zip、kernel、rootfs）到指定目录。 | 参考 HiSH README 中的下载链接。 |
| 1.4 | 在 DevEco Studio 中构建项目，并在真机或模拟器上运行，确保 HiSH 终端正常显示。 | 验证签名和权限配置无误。 |
| 1.5 | 熟悉 HiSH 源码结构，重点标记以下文件（后续需要引用但不修改）：<br>- `entry/src/main/ets/pages/Index.ets`（仅修改入口路由）<br>- `entry/src/main/ets/components/WebTerminal.ets`（保持不变）<br>- `entry/src/main/ets/lib/startVm.ets`（保持不变） | 建立代码理解基础。 |

**阶段产出**：可运行的 HiSH 开发环境。

---

### 阶段二：新增 IDE 布局页面（预计 2 天）

**目标**：创建一个全新的布局页面 `IdeLayout.ets`，采用 VSCode 风格的三栏可拖拽布局，并将原有的 `WebTerminal` 嵌入右下区域，**不修改 `WebTerminal` 的任何代码**。

| 步骤 | 具体操作 | 最小改动措施 |
| :--- | :--- | :--- |
| 2.1 | 在 `entry/src/main/ets/pages/` 下新建文件 `IdeLayout.ets`，定义入口组件 `IdeLayout`。 | ✅ 新建文件，不修改已有文件。 |
| 2.2 | 实现 `RowSplit` + `ColumnSplit` 嵌套布局：左侧为文件树占位，右上为编辑器占位，右下为终端区域。 | 不涉及原有代码。 |
| 2.3 | 将原有的 `WebTerminal` 组件导入并放置到右下区域，传递相同的参数（`controller`、`onReady` 等）。 | 直接复用，无需修改 `WebTerminal`。 |
| 2.4 | 修改 `Index.ets` 中的 `build()` 方法，增加设备类型或启动参数判断，当满足条件时跳转到 `IdeLayout` 而非原布局。 | **仅修改 `Index.ets` 的 `build` 函数，增加一个条件分支，改动量不超过 5 行。** |
| 2.5 | 测试终端在缩小容器中是否正常工作，如有尺寸适配问题，可在父容器变化时调用终端的 `resize` 方法（通过 `@Watch` 监听尺寸变化）。 | 不修改终端内部，仅在外层调用其公有方法。 |

**阶段产出**：在保留原 HiSH 功能的同时，新增一个 IDE 布局页面，终端在右下区域正常运行，其余区域为占位符。

---

### 阶段三：集成代码编辑器（预计 1 天）

**目标**：在右上区域集成 HMCodeEditor，实现代码编辑基本功能，并支持多语言语法高亮。

| 步骤 | 具体操作 | 最小改动措施 |
| :--- | :--- | :--- |
| 3.1 | 在 `entry/oh-package.json5` 的 `dependencies` 中添加 `"hm_code_editor": "^1.0.0"`。 | 仅修改依赖文件，不影响原有功能。 |
| 3.2 | 在 `IdeLayout.ets` 中导入 `HMCodeEditor`，并将其放置到右上区域。 | 修改新建的 `IdeLayout.ets`，不触碰原有文件。 |
| 3.3 | 配置编辑器属性（主题、字体、行号等），并绑定内容变化事件到 `@State currentContent`。 | 全部在新建文件中完成。 |
| 3.4 | 测试编辑器加载，确保可输入代码，并尝试切换语言（如 JavaScript / ArkTS / JSON）。 | 无侵入性。 |

**阶段产出**：编辑器正常工作，与终端共存于 IDE 布局中。

---

### 阶段四：文件管理（侧边栏）与标签栏（预计 3 天）

**目标**：在左侧实现文件树，在顶部实现标签栏，支持文件浏览、打开、切换和关闭，**所有新功能均在新建组件中实现**。

| 步骤 | 具体操作 | 最小改动措施 |
| :--- | :--- | :--- |
| 4.1 | 新建 `FileTree.ets` 组件，利用 `@ohos.file.fs` 读取指定目录，构建树形数据结构。 | ✅ 新建文件。 |
| 4.2 | 使用 `List` + 递归 `@Builder` 渲染文件树，支持文件夹展开/收缩。 | 独立组件，不影响其他部分。 |
| 4.3 | 新建 `EditorTabs.ets` 组件，管理已打开文件的标签列表，支持点击切换和关闭。 | 新建文件。 |
| 4.4 | 在 `IdeLayout` 中组合这两个组件，并实现点击文件 → 打开标签 → 更新编辑器内容的完整流程。 | 修改仅限 `IdeLayout.ets`（新建文件）。 |
| 4.5 | 使用 `fs.readText` 读取文件内容，`fs.writeText` 保存内容（可绑定快捷键）。 | 调用系统 API，无侵入。 |
| 4.6 | 设置默认工作目录（建议使用应用沙箱的 `files` 目录或用户选择的目录），避免依赖 `/mnt/share` 的虚拟路径。 | 配置外部，不影响原 HiSH 的共享目录机制。 |

**阶段产出**：侧边栏可浏览文件，点击文件在编辑器打开，标签栏显示已打开文件，支持多标签切换和关闭。

---

### 阶段五：终端与编辑器联动（预计 1 天）

**目标**：实现“运行当前文件”功能，将编译/执行命令发送到终端。

| 步骤 | 具体操作 | 最小改动措施 |
| :--- | :--- | :--- |
| 5.1 | 在 `WebTerminal` 外层封装一个 `TerminalController`，提供 `sendCommand` 方法（如果 HiSH 未提供，则通过 `WebTerminal` 内部的 `QemuAgent` 发送）。 | 若需调用 `QemuAgent`，可在新建的封装类中引用，不修改原有类。 |
| 5.2 | 在编辑器工具栏或菜单中添加“运行”按钮，根据当前文件类型构造命令（如 `gcc`、`node`、`python3` 等）。 | 在 `IdeLayout` 中实现，不涉及原有文件。 |
| 5.3 | 将命令通过 `sendCommand` 发送到终端，终端自动执行并显示输出。 | 无侵入。 |
| 5.4 | 增加快捷键支持（如 Ctrl+Enter）。 | 在新建页面中处理键盘事件。 |

**阶段产出**：用户可以从编辑器一键编译/运行代码，终端实时反馈结果。

---

### 阶段六：用户体验优化与打磨（预计 2 天）

**目标**：完善界面细节，提升整体可用性，所有优化均在新建文件和配置中完成。

| 步骤 | 具体操作 | 最小改动措施 |
| :--- | :--- | :--- |
| 6.1 | 调整分隔条样式（颜色、粗细），使其更接近 VSCode。 | 在 `IdeLayout` 的 `RowSplit`/`ColumnSplit` 中配置 `.divider()`。 |
| 6.2 | 为文件树添加图标（使用系统资源或 SymbolGlyph）。 | 在 `FileTree` 中处理。 |
| 6.3 | 增加“新建文件/文件夹”功能（通过按钮或右键菜单）。 | 在 `FileTree` 中添加相应按钮。 |
| 6.4 | 实现“自动保存”或“保存（Ctrl+S）”功能。 | 在 `IdeLayout` 中监听编辑器事件和快捷键。 |
| 6.5 | 适配不同屏幕尺寸（手机、平板、PC），确保布局合理。 | 使用媒体查询在 `IdeLayout` 中动态调整尺寸。 |
| 6.6 | 测试多文件编辑、大文件加载、终端长时间运行等场景，修复性能问题。 | 如有必要，可优化 `FileTree` 使用 `LazyForEach`，不涉及原有代码。 |

**阶段产出**：产品级可用的 IDE，体验流畅，功能完整。

---

## 4. 改动文件清单（最小化）

| 文件路径 | 操作类型 | 修改内容 |
| :--- | :--- | :--- |
| `entry/src/main/ets/pages/Index.ets` | **修改（极少）** | 在 `build()` 方法中增加一个条件分支，调用 `IdeLayout`。 |
| `entry/oh-package.json5` | **修改（添加依赖）** | 添加 `hm_code_editor` 依赖。 |
| `entry/src/main/ets/pages/IdeLayout.ets` | **新建** | 完整 IDE 布局和逻辑。 |
| `entry/src/main/ets/components/FileTree.ets` | **新建** | 文件树组件。 |
| `entry/src/main/ets/components/EditorTabs.ets` | **新建** | 标签栏组件。 |
| `entry/src/main/ets/utils/FileUtils.ets` | **新建** | 文件读写工具函数。 |
| `entry/src/main/ets/controller/TerminalController.ets` | **新建**（可选） | 封装终端控制接口，若 HiSH 未暴露 sendCommand 方法。 |
| `entry/src/main/resources/base/media/` | **新增图标资源** | 添加文件夹、文件等图标（不影响原有资源）。 |

**总计改动/新增文件不超过 8 个，其中核心修改仅在 `Index.ets` 一处（约 3~5 行），其余均为新增文件。**

---

## 5. 风险与应对措施

| 风险 | 影响 | 应对策略 |
| :--- | :--- | :--- |
| HMCodeEditor 与 HiSH 使用的 ArkTS 版本不兼容 | 编辑器无法加载 | 查阅 HMCodeEditor 的兼容性说明，必要时改用 `Web` 组件加载 Monaco Editor CDN，且此操作也在新建文件中完成。 |
| 终端在缩小的容器中渲染异常（如键盘输入错位） | 终端交互不可用 | 检查 `WebTerminal` 是否监听了尺寸变化，若未提供，可在外层通过 `onAreaChange` 主动调用其 `resize` 方法（如果暴露）。不修改其内部。 |
| 文件系统权限不足，无法读取共享目录 | 文件树不可用 | 默认使用应用沙箱目录 `context.filesDir`，并允许用户通过文件选择器选择其他目录，此逻辑在新建文件中处理。 |
| 虚拟机资源占用高，卡顿 | 用户体验差 | 提供选项让用户调整 CPU/内存分配（通过修改 `startVm.ets` 的参数），此修改将作为配置扩展，可独立进行（需改动 `startVm.ets`，但可封装为配置参数，尽量少改）。 |
| 包体积过大（>300MB） | 分发困难 | 维持原 HiSH 的包体策略，提示用户通过 WiFi 下载；可考虑将资源放在外部存储（但需额外开发，优先级低）。 |

---

## 6. 里程碑时间线

| 阶段 | 预计工时 | 累计天数 |
| :--- | :--- | :--- |
| 阶段一：环境搭建 | 1 天 | 第 1 天 |
| 阶段二：新增 IDE 布局 | 2 天 | 第 3 天 |
| 阶段三：集成编辑器 | 1 天 | 第 4 天 |
| 阶段四：文件管理+标签栏 | 3 天 | 第 7 天 |
| 阶段五：终端联动 | 1 天 | 第 8 天 |
| 阶段六：打磨优化 | 2 天 | 第 10 天 |

**总计约 10 个工作日**，即可完成一个功能完备、且对原 HiSH 代码改动极小的鸿蒙 IDE。

---

## 7. 后续可扩展方向

- 支持 Git 版本控制（通过调用命令行或集成库）
- 代码智能补全（集成 LSP）
- 远程开发（SSH 连接）
- 插件体系（类似 VSCode 扩展）

---

**本计划书严格遵循“最小改动”原则，所有新增功能均在新建文件和配置中实现，最大限度保护原有 HiSH 代码的完整性和可维护性。**